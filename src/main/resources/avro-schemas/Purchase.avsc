{
  "namespace": "dns.demo.kafka.domain",
  // This field seems to be ignored. The compatibility still needs to be set to FORWARD in the topic (default is always BACKWARD)
  "compatibility": "FORWARD",
  "type": "record",
  "name": "Purchase",
  "fields": [
    {
      "name": "id",
      "type": "int"
    },
    {
      "name": "product",
      "type": "string"
    },
    {
      "name": "quantity",
      "type": "int"
    },
    {
      "name": "member_id",
      "type": "int"
    }
  ]
}